global:
  scrape_interval: 15s

scrape_configs:
  - job_name: socratic_tutor
    metrics_path: /metrics
    static_configs:
      - targets:
          - fastapi:8000
        labels:
          env: docker
